package api

// TODO: replace with JWT, some kind of authentication token
func AuthenticateUser() (string, error) {
	/*
		if there is an error, let the
		authentication token be nil
	*/

	return "abcdefgh", nil
}

// TODO: replace with User model
func GetUser(id string) string {
	return id
}
